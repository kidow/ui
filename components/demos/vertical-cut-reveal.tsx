'use client'

import VerticalCutReveal from '@/components/kidow/vertical-cut-reveal'

export default function VerticalCutRevealDemo() {
  return (
    <div className="flex min-h-64 w-full items-center justify-center overflow-hidden p-2">
      <VerticalCutReveal>한 곳에 모은 컴포넌트</VerticalCutReveal>
    </div>
  )
}
