'use client'

import MetisText from '@/components/kidow/metis-text'

export default function MetisTextDemo() {
  return (
    <div className="flex min-h-64 w-full items-center justify-center overflow-hidden p-2">
      <MetisText>한 곳에 모은 컴포넌트</MetisText>
    </div>
  )
}
