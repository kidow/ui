import { NoiseTexture } from '@/components/kidow/noise-texture-componentry'

export default function NoiseTextureComponentryDemo() {
  return (
    <div className="relative h-64 w-full overflow-hidden rounded-lg border">
      <NoiseTexture />
    </div>
  )
}
