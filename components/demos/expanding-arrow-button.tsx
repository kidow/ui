'use client'

import { ExpandingArrowButton } from '@/components/kidow/expanding-arrow-button/components/motion/expanding-arrow-button'

export default function ExpandingArrowButtonDemo() {
  return (
    <div className="flex min-h-64 w-full items-center justify-center overflow-hidden p-2">
      <ExpandingArrowButton>한 곳에 모은 컴포넌트</ExpandingArrowButton>
    </div>
  )
}
